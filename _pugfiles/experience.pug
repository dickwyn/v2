section#experience
    .resume-wrapper
        .container
            div
                .resume-heading
                    |   Experience
            div
                |   {% for experience in site.data.experiences %}
                .resume-wrapper-nested
                    .resume-subheading
                        |   {{ experience.role }}
                    .time
                        |   {{ experience.time }}  
                    .resume-subsubheading
                        |   {{ experience.company }}
                |   {% endfor %}